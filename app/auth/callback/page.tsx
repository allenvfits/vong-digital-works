import OAuthCallback from "./OAuthCallback";

export const metadata = { title: "Completing Sign In | Vong Digital Works", robots: { index: false, follow: false } };

export default function AuthCallbackPage() {
  return <OAuthCallback />;
}
