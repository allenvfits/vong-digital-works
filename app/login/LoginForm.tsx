"use client";
import { FormEvent, useState } from "react";
export default function LoginForm(){const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const form=new FormData(e.currentTarget);const response=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:form.get("email"),password:form.get("password")})});const data=await response.json();if(response.ok){location.href=data.destination||"/portal";return}setError(data.error||"Sign in failed");setLoading(false)}return <form className="loginCard" onSubmit={submit}><a className="brand" href="/"><span>V</span> VONG / DIGITAL WORKS</a><p className="kicker">Secure account access</p><h1>Sign in to your<br/>private workspace.</h1><label>Email<input name="email" type="email" required autoComplete="email"/></label><label>Password<input name="password" type="password" required autoComplete="current-password"/></label>{error&&<p className="loginError">{error}</p>}<button className="dashAdd" disabled={loading}>{loading?"Signing in…":"Sign in →"}</button><small>Approved team members enter the business dashboard. Customers enter a private portal containing only their own records.</small></form>}
